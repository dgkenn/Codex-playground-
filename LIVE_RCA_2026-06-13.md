# RCA: live BTC bot losing day (2026-06-13) — bot SHUT OFF

Operator: "huge losing day... shut it off, then find why." DONE: LIVE_SWITCH=off (commit 709bcd7).
Reconstructed the FULL day from the live-state branch history (19 cycles overwrite one file/cycle;
walked all commits, deduped by trade_id).

## The numbers (517 fills, 56 windows 00:30-14:15Z, all settled)
- Realized DAY P&L = **-$3.07** on $247 turnover (244 boxes, ~$1/box, unit size). Small in absolute
  $ at $5-notional, but a STRUCTURAL bleed (loss-limit $6 not tripped, so it ran all day).
- **Decomposition is decisive:**
  - BOXES (paired): +$3.75, **+1.54c/box** — the maker box edge WORKS, exactly as designed.
  - STRANDS (unpaired legs): 29 strands x **-21.76c = -$6.31** — this is the ENTIRE loss + more.
- **100% MAKER fills (0 taker)** — the bot is NOT crossing the spread; no fee/crossing leak.

## Root cause: the LIVE strand rate is ~4x the backtest, in a CALM market
- **48% of windows stranded live** vs **~13% modeled OOS**. And it was NOT a tail regime: BTC range
  $356 (0.6%), mean |sig| 3.5bps — a flat, choppy day. Strands split 15 short / 12 long (chop, not trend).
- So the boxes earn +1.5c but strands (4x too frequent, -21.76c each) overwhelm them. Break-even needs
  <~17 strands (244*1.54c / 21.76c); we had 29.
- WHY 4x: the backtest FILL MODEL is too OPTIMISTIC. In live 1c-spread chop, when price ticks, one leg
  of our two-sided quote gets adversely lifted and the other leg's resting quote is now stale/away ->
  strand. The tape-replay backtests (which every study flagged as "optimistic fills") UNDERSTATE this.
  The live maker is adversely selected far more than the replay assumed. This is a MODEL-vs-LIVE GAP,
  and it means the backtested "+EV box+ladder" does NOT survive real fill dynamics at this rate.
- Layering amplifies it: the bot does 4-9 fills/window (multiple boxes to fill the $5 notional); each
  extra box is another chance to end the window imbalanced -> more strands.

## Contributing: the streak-guard removal (TODAY)
Earlier today we REMOVED --strand-scaledown (the post-strand cool-off) because the OOS ablation said it
was net-negative (+0.61c/win to drop). BUT that verdict was on the SAME optimistic-fill backtest, and we
explicitly noted the guard was a TAIL/drawdown REDUCER. Today is the tail it protected against: with the
cool-off gone, the bot kept full participation through the high-strand chop instead of scaling down. The
removal did not CAUSE the 4x strand rate (that's the fill-model gap), but it removed the one live brake.

## Recommendations (bot stays OFF until these are addressed)
1. **REVERT the streak-guard removal** — restore --strand-scaledown. Today proves its tail protection is
   worth the small modeled average cost; the ablation that said "drop it" trusted an optimistic fill model
   that we now know understates live strands.
2. **Trust LIVE over backtest.** The live strand rate (48%) is the truth; our backtests are optimistic.
   Re-baseline the box+ladder economics using the LIVE strand rate, not the tape. The strategy may be
   ~breakeven-to-negative at the true rate -> do NOT re-arm at size until net-positive on LIVE data.
3. **Investigate the strand mechanism** (instrument): the fills carry ctx.resting_s, win_fills, t_in_win.
   Quantify WHY the 2nd leg fails to fill (how long it rested, how far the touch moved) -> fix the quoting
   (e.g. re-quote the lagging leg faster, or tighter pairing, or 1-box-per-window to cut layering strands).
4. **Cut participation / layering**: cap to fewer boxes per window (less imbalance exposure) and/or widen
   the open gate; fewer, cleaner boxes beat many strand-prone ones.
5. **Disposal**: -21.76c/strand is high -> the completion/sell-cheap rung isn't capping live strand losses;
   tighten force-complete / flatten on the live path.

## Honest meta-finding
The boxes genuinely earn the spread (+1.5c/box, all maker) — the core idea is sound. The killer is that
LIVE strands run ~4x the backtested rate, so the net flips negative. Our entire backtest suite shares an
optimistic maker-fill assumption; the live tape is the ground truth and it says: handle strands harder
(restore the cool-off, cut layering, fix 2nd-leg fills) before re-enabling, and re-validate on LIVE data.

## >>> STRAND-MECHANISM INVESTIGATION + RE-BASELINE (update) <<<
### Mechanism: the live trader CANNOT dispose of strands (architecture, not market)
Code proof (kalshi_trader.py): place_order is BUY-ONLY with post_only=True (lines 398/411/937).
Live-fill proof: 0 sells / 0 takers out of 517 fills. Consequences:
- CANNOT FLATTEN a stranded leg (no sell-side) -> close-flatten-tau never sells (0 sells observed).
- CANNOT CROSS to complete (post_only rejects crossing) -> chase-unpaired posts a non-crossing maker
  bid that doesn't fill when the touch moves -> strand persists.
- _flatten_and_exit = cancel-all (cancels resting ORDERS), NOT liquidate POSITIONS -> held legs settle.
=> Every strand is HELD NAKED TO BINARY SETTLEMENT (the worst disposal: hold -6.84c vs complete -1.07c
   in backtest; live -21.76c). The entire complete/sell-cheap/hedge ladder we researched assumed
   crossing/selling the live bot STRUCTURALLY CANNOT DO. Live = pure box-legging, strands unhandled.
Secondary mechanism stats: layering ~9 fills/win (same for paired & stranded); 12/56 windows hit
|net|>=2 (runaway one-sided fills); stranded legs mildly adversely-selected (post-fill drift -0.4c)
but the loss is the BINARY SETTLEMENT of the held leg, not the drift.

### Re-baseline (live-calibrated; 244 boxes @ +1.54c, 29 strands, 56 windows)
- AS-DEPLOYED (strands held): boxes +$3.76, strands -$6.31 => DAY -$2.55 (-4.6c/win). This is the TRUE
  economics of the as-deployed (disposal-disabled) bot -- NOT the researched ladder.
- IF DISPOSAL WORKED (cross-to-flatten ~-5c/strand incl ~2c taker fee): DAY +$2.31 (+4.1c/win) -> FLIPS POSITIVE.
  Pessimistic -10c/strand: +$0.86. Even crossing-with-fees beats holding for -21.76c by a mile.
- + cut strand RATE 48%->30% (restore streak guard + less layering): +$2.3 to +$2.9/day.
- BREAK-EVEN: strand cost > -13c (we hold at -21.76c) OR strand rate < 31% (we're at 48%). Either lever
  alone roughly crosses zero; both together gives a healthy margin.

### Revised priority fixes (bot stays OFF until #1 is implemented + tested)
1. **WIRE STRAND DISPOSAL into the live trader (THE fix):** give it the ability to FLATTEN a stranded
   leg -- either place SELL orders, or allow the completion-chase to CROSS (drop post_only for the
   disposal path), accepting the ~1-2c taker fee. This is the -21.76c -> ~-5c lever; it alone flips the
   day to ~+$2.3. (Real money-path code change -> implement carefully + paper/DRY test first.)
2. **Restore the streak cool-off** + cap layering / |net|>=2 to cut the strand RATE (48%->~30%).
3. **Re-validate on LIVE data** (the 48% live strand rate, not the 13% optimistic tape) before re-arming.
RECLASSIFY: this losing day was NOT proof the strategy is -EV. It's proof the DEPLOYED bot is missing
its disposal rung (post-only-buy-only disables it). The researched ladder, if actually executable live,
re-baselines to ~+$2-3/day at current size. The gate to re-enable is: disposal wired + net-positive on live.

## >>> FIXES IMPLEMENTED (1,2,3) — bot stays OFF pending live re-validation <<<
Commits: trader a7dfbf7; live.yml main 1a31a0c / code 5f3a8ca / continuation 69b14cc.

**FIX 1 — strand DISPOSAL wired (the -21.76c -> ~-5c lever).** New `--dispose-cross` in kalshi_trader.py:
when a leg stays unpaired past --chase-unpaired-s (45s) OR within --close-flatten-tau (120s) of close,
the trader now CROSSES to take the offer and COMPLETE the box (place_order post_only=False; place()
gains a cross path that skips the post-only guard), bounded by --chase-max-give (2c) / --close-max-give
(4c). This is the rung the post-only-only trader structurally could not execute. Default OFF; armed in live.yml.

**FIX 2 — strand RATE cut (dramatic).** live.yml now sets `--max-fills-side 3` (cap OPENING fills/side;
completions exempt) and restores `--strand-scaledown "0.75,0.5,0.25"` (post-strand cool-off). On today's
live data this cuts the strand rate 48% -> 11% (sim). Chosen cap=3 as the robust sweet spot: +$0.87/day
if disposal works AND only -$0.30 worst-case if disposal underperforms (vs -$2.56 uncapped/held).

**Projected (live-calibrated, today's fills):** cap=3 + dispose-cross => ~+$0.87/day (+1.5c/win), bounded
downside -$0.30. Uncapped+disposal is higher (+$2.30) but cap=3 is robust to the -5c disposal assumption
being wrong. (box edge is real +1.54c/box, all maker.)

**FIX 3 — LIVE RE-VALIDATION GATE (execute on re-arm; bot stays OFF until then).** The fix is compile-
and DRY-clean but a strand+cross cannot be exercised in DRY (no fills booked), so the real test is the
first live cycles. RE-ARM CAUTIOUSLY and watch the first ~50-100 windows for ALL of:
  (a) STRAND RATE drops to ~10-15% (from 48%) -- count windows ending |net|!=0 in the fees log.
  (b) DISPOSE-CROSS FIRES -- look for "[DISPOSE-CROSS]" log lines AND fills with raw.is_taker=true
      (today had 0 takers; the fix should produce a few taker completions).
  (c) per-strand realized loss drops from -21.76c toward ~-5 to -10c (disposed, not held to settle).
  (d) DAY P&L turns non-negative.
GO/NO-GO: keep on (and consider scaling) only if (a)+(b)+(c) hold and net >= ~0 over the sample; else
flip off and re-diagnose. The $6 sticky loss-limit + dead-man remain as hard backstops. Do NOT scale
size until net-positive on live with the new config.
