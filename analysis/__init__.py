"""Phase-1 analysis on the compact tables only (disk-light, Sec 8-11).

All of these operate on the persisted embedding/feature tables -- never on raw
signal. NumPy / scikit-learn are imported lazily inside functions.
"""
