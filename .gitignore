# local Claude Code harness state (settings, worktree admin) -- not project content
.claude/*
!.claude/skills/
__pycache__/

# stray agent working artifacts
extra.log
scratchpad/
.kalshi_cache/
.nowcast_cache/
.predexon_key
.secrets
*.key
.env
.settlement_basis_cache/
.tailrisk_cache/
.kalshi_creds
kwx_exec_log.jsonl
.orderbook_cache/
kwx_forward_settled.jsonl
kwx_runner_state.json
kwx_runner_plan.jsonl
.synoptic_token
.phase3_feed_cache/
kwx_gate_status.txt
kwx_gate_run.log
wx_forecast_paper.jsonl
wx_forecast_settled.jsonl

# early-lock study cache (agent artifact, do not commit)
_earlylock_cache/

# Polymarket gap-study cache (raw ASOS/CLOB pulls, agent artifact, do not commit)
.pmkt_cache/

# pmkt settlement-basis study caches (agent artifact, do not commit)
_pmkt_cache/
_pmkt_basis_cache/
pmkt_settlement_basis_results.json

# pmkt final-verdict (phase 2) cache -- raw IEM/Gamma/CLOB pulls, agent artifact, do not commit
.pmkt_final_cache/
# early-lock forward PAPER logs: force-added by the kwx-earlylock workflow (git add -f), like the forecast
# sleeve, so they accumulate on the branch without cluttering local `git status`. The committed
# _earlylock_climatology.json (NOT ignored) is what live snapshots load.
wx_earlylock_paper.jsonl
wx_earlylock_settled.jsonl

# synoptic trial detection-latency log: force-added by the kwx-synoptic-trial workflow (git add -f),
# same pattern as the forecast/earlylock sleeves above.
wx_synoptic_trial.jsonl

# structured book snapshots: force-added by the kwx-depthprobe workflow (git add -f), same pattern as the
# early-lock/forecast paper logs -- accrues on the branch without cluttering local `git status`. Schema:
# wx_book_snapshot_schema.md (committed, NOT ignored).
wx_book_snapshots.jsonl
