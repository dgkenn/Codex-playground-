# Python virtual environment (created for pmkit/pmxt backtesting work)
.venv/
__pycache__/
*.pyc

# Large data artifacts — never commit market data dumps
*.parquet
*.npz

# Run logs
*.log

# Secrets & local state (NEVER commit)
.env
*.db
*.sqlite
secrets/
order_log.jsonl
fills_log.jsonl
reprice_log.jsonl
live_markout.jsonl
live_metrics_*.jsonl
queue_jump_log.jsonl
box_arb_log.jsonl
queue_jump_log.jsonl
makers_trades.jsonl
makers_agg.json
wallet_track.jsonl
wallet_trades.parquet
makers_trades.jsonl
makers_agg.json
copy_capture.jsonl
copy_orders.jsonl
mm_score.json
copy_result.json
copy_health.json
copy_autotune.jsonl
copy_all_results.json
copy_multi_result.json
copy_multi_capture.jsonl
strategy_models.json
strategy_fine.jsonl

# Paper-trading data pulled locally for analysis (git checkout origin/gha-data -- gha_data/).
# It lives ONLY on the dedicated gha-data branch -- never commit it to the code branch.
# (Files already tracked in history remain tracked; this only keeps fresh pulls out of status.)
gha_data/

# private keys — NEVER commit (RSA PEMs, etc.)
*.pem
*.key
.kalshi/
live_metrics_kalshi_*.jsonl
kalshi_markout.jsonl
.kalshi_killed_*
overnight_data/
kalshi_fees_*.jsonl
.scorecard_results.json
