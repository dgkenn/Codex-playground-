numpy==2.4.6
scipy==1.17.1
pandas==3.0.3
pyarrow==24.0.0
duckdb==1.5.3
requests==2.34.2
websockets==16.0
pytz==2026.2

# LIVE execution only (not needed for research/paper; install on your trading infra) -- see PILOT.md/DEPLOY.md
# py-clob-client-v2   # Polymarket CLOB order placement (EIP-712 / POLY_1271 signing)
# web3                # on-chain mint/merge via the ConditionalTokens / neg-risk adapter
# coincurve           # libsecp256k1 signing backend -> eth_account auto-uses it; EIP-712 sign <1ms
#                     #   (vs several ms pure-Python). The single biggest sub-10ms win after co-location.
